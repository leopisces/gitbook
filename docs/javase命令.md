# JavaSE 相关命令


```js
# 在项目中添加 mvnw(Maven Wrapper，下文都用mvnw指代Maven Wrapper)
$ mvn -N io.takari:maven:wrapper

# 加上-Dmaven参数来指定wrapper使用的Maven版本，例如我们需要maven 3.3.3版本：
mvn -N io.takari:maven:wrapper -Dmaven=3.3.3

```